import './App.css';
import { Button } from '@/components/ui/button';

function App() {
	return (
		<>
			<h1>Vite + React + TS</h1>
			<div className="card">
				<Button variant="default">Button</Button>
			</div>
		</>
	);
}

export default App;
